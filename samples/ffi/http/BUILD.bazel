# AUTO-GENERATED by tools/bazel/translate_gn_desc.py from `gn desc` output.
# Hand-fixes belong in this file (M3); rerun the translator to refresh structural
# bits and re-apply fixes from version control.

load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library", "cc_shared_library")

package(default_visibility = ["//visibility:public"])

# GN lets one target's sources reach into other BUILD.gn dirs; Bazel
# requires cross-package file refs to be explicitly exported. Bulk-export
# every file in the package so the gn-shaped source lists keep working.
exports_files(glob(
    ["**/*.h", "**/*.hpp", "**/*.hh", "**/*.inc",
     "**/*.cc", "**/*.cpp", "**/*.c", "**/*.S", "**/*.s"],
    allow_empty = True,
))

# GN: shared_library("fake_http") { sources = ["lib/fake_http.cc"] }.
# Compile the source into a normal cc_library (provides CcInfo, so aggregate
# cc_library consumers like //:runtime keep working). alwayslink keeps the
# DART_EXPORT FFI entry points (only reached via dlopen) from being dropped.
cc_library(
    name = "fake_http",
    srcs = ["lib/fake_http.cc"],
    # -fPIC: this custom toolchain doesn't advertise supports_pic, so Bazel
    # emits only non-PIC objects by default; force PIC so the objects can link
    # into libfake_http.so (gn shared_library compiles PIC implicitly).
    copts = [
        "-Isamples/ffi/http",
        "-fPIC",
    ],
    alwayslink = True,
)

# The real shared object FFI samples dlopen by name. This subsumes the gn
# type=copy node (fake_http_copy): cc_binary(linkshared) already emits the
# correctly-named .so directly.
cc_binary(
    name = "libfake_http.so",
    copts = ["-Isamples/ffi/http"],
    linkshared = True,
    linkopts = ["-rdynamic"],
    deps = [":fake_http"],
)

