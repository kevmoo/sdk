# AUTO-GENERATED by tools/bazel/translate_gn_desc.py from `gn desc` output.
# Hand-fixes belong in this file (M3); rerun the translator to refresh structural
# bits and re-apply fixes from version control.

load("//tools/bazel:rules.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

# GN lets one target's sources reach into other BUILD.gn dirs; Bazel
# requires cross-package file refs to be explicitly exported. Bulk-export
# every file in the package so the gn-shaped source lists keep working.
exports_files(glob(
    [
        "**/*.h",
        "**/*.hpp",
        "**/*.hh",
        "**/*.inc",
        "**/*.cc",
        "**/*.cpp",
        "**/*.c",
        "**/*.S",
        "**/*.s",
    ],
    allow_empty = True,
))

# TODO(M3): genrule for build_sdk_tar (gn type=action)
filegroup(name = "build_sdk_tar")

# TODO(M3): copy for copy_dart_sdk_js (gn type=copy)
filegroup(name = "copy_dart_sdk_js")

# TODO(M3): copy for copy_dart_sdk_js_map (gn type=copy)
filegroup(name = "copy_dart_sdk_js_map")

# TODO(M3): copy for copy_ddc_module_loader (gn type=copy)
filegroup(name = "copy_ddc_module_loader")

# TODO(M3): copy for copy_sandbox_js (gn type=copy)
filegroup(name = "copy_sandbox_js")

# TODO(M3): copy for copy_worker_loader (gn type=copy)
filegroup(name = "copy_worker_loader")

cc_library(
    name = "dartpad",
    deps = [
        "//utils/dartpad:build_sdk_tar",
        "//utils/dartpad:copy_dart_sdk_js",
        "//utils/dartpad:copy_dart_sdk_js_map",
        "//utils/dartpad:copy_ddc_module_loader",
        "//utils/dartpad:copy_sandbox_js",
        "//utils/dartpad:copy_worker_loader",
        "//utils/dartpad:worker",
    ],
)

# TODO(M3): genrule for worker (gn type=action)
filegroup(name = "worker")
