# AUTO-GENERATED by tools/bazel/translate_gn_desc.py from `gn desc` output.
# Hand-fixes belong in this file (M3); rerun the translator to refresh structural
# bits and re-apply fixes from version control.

load("//tools/bazel:rules.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

# GN lets one target's sources reach into other BUILD.gn dirs; Bazel
# requires cross-package file refs to be explicitly exported. Bulk-export
# every file in the package so the gn-shaped source lists keep working.
exports_files(glob(
    [
        "**/*.h",
        "**/*.hpp",
        "**/*.hh",
        "**/*.inc",
        "**/*.cc",
        "**/*.cpp",
        "**/*.c",
        "**/*.S",
        "**/*.s",
    ],
    allow_empty = True,
))

cc_library(
    name = "dart_mcp_server_aot",
    deps = [
        "//utils/dart_mcp_server:dart_mcp_server_aot_product_snapshot",
        "//utils/dart_mcp_server:dart_mcp_server_aot_snapshot",
    ],
)

# TODO(M3): copy for dart_mcp_server_aot_product_snapshot (gn type=copy)
cc_library(name = "dart_mcp_server_aot_product_snapshot")

# TODO(M3): genrule for dart_mcp_server_aot_product_snapshot_dill (gn type=action)
cc_library(name = "dart_mcp_server_aot_product_snapshot_dill")

# TODO(M3): genrule for dart_mcp_server_aot_product_snapshot_gen_snapshot (gn type=action)
cc_library(name = "dart_mcp_server_aot_product_snapshot_gen_snapshot")

# TODO(M3): copy for dart_mcp_server_aot_snapshot (gn type=copy)
cc_library(name = "dart_mcp_server_aot_snapshot")

# TODO(M3): genrule for dart_mcp_server_aot_snapshot_dill (gn type=action)
cc_library(name = "dart_mcp_server_aot_snapshot_dill")

# TODO(M3): genrule for dart_mcp_server_aot_snapshot_gen_snapshot (gn type=action)
cc_library(name = "dart_mcp_server_aot_snapshot_gen_snapshot")
