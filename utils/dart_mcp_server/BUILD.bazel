# AUTO-GENERATED by tools/bazel/translate_gn_desc.py from `gn desc` output.
# Hand-fixes belong in this file (M3); rerun the translator to refresh structural
# bits and re-apply fixes from version control.

load("@rules_cc//cc:defs.bzl", "cc_library")
load("//tools/bazel/dart:defs.bzl", "dart_aot_snapshot")

package(default_visibility = ["//visibility:public"])

# GN lets one target's sources reach into other BUILD.gn dirs; Bazel
# requires cross-package file refs to be explicitly exported. Bulk-export
# every file in the package so the gn-shaped source lists keep working.
exports_files(glob(
    [
        "**/*.h",
        "**/*.hpp",
        "**/*.hh",
        "**/*.inc",
        "**/*.cc",
        "**/*.cpp",
        "**/*.c",
        "**/*.S",
        "**/*.s",
    ],
    allow_empty = True,
))

# rules_dart Step 4. main is same-package (utils/dart_mcp_server/dart_mcp_server.dart),
# so it's referenced directly, no root exports_files needed. Group carries the
# snapshots via `data`.
cc_library(
    name = "dart_mcp_server_aot",
    data = [
        "//utils/dart_mcp_server:dart_mcp_server_aot_product_snapshot",
        "//utils/dart_mcp_server:dart_mcp_server_aot_snapshot",
    ],
)

# Ports utils/dart_mcp_server/BUILD.gn:aot_snapshot("dart_mcp_server_aot_product_snapshot").
dart_aot_snapshot(
    name = "dart_mcp_server_aot_product_snapshot",
    out = "dart_mcp_server_aot_product.dart.snapshot",
    gen_kernel_dill = "//utils/gen_kernel:bootstrap_gen_kernel",
    gen_snapshot = "//runtime/bin:gen_snapshot_product",
    main = "dart_mcp_server.dart",
    platform_dill = "//runtime/vm:vm_platform.dill",
    product = True,
    sources = "//:dart_pkg_dart_mcp_server",
)

# Ports utils/dart_mcp_server/BUILD.gn:aot_snapshot("dart_mcp_server_aot_snapshot").
dart_aot_snapshot(
    name = "dart_mcp_server_aot_snapshot",
    out = "dart_mcp_server_aot.dart.snapshot",
    gen_kernel_dill = "//utils/gen_kernel:bootstrap_gen_kernel",
    gen_snapshot = "//runtime/bin:gen_snapshot",
    main = "dart_mcp_server.dart",
    platform_dill = "//runtime/vm:vm_platform.dill",
    product = False,
    sources = "//:dart_pkg_dart_mcp_server",
)
