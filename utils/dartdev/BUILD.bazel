# AUTO-GENERATED by tools/bazel/translate_gn_desc.py from `gn desc` output.
# Hand-fixes belong in this file (M3); rerun the translator to refresh structural
# bits and re-apply fixes from version control.

load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library", "cc_shared_library")
load("//tools/bazel/dart:defs.bzl", "dart_aot_snapshot")

package(default_visibility = ["//visibility:public"])

# GN lets one target's sources reach into other BUILD.gn dirs; Bazel
# requires cross-package file refs to be explicitly exported. Bulk-export
# every file in the package so the gn-shaped source lists keep working.
exports_files(glob(
    ["**/*.h", "**/*.hpp", "**/*.hh", "**/*.inc",
     "**/*.cc", "**/*.cpp", "**/*.c", "**/*.S", "**/*.s"],
    allow_empty = True,
))

# TODO(M3): copy for copy_dartdev_snapshot (gn type=copy)
cc_library(name = "copy_dartdev_snapshot")

# TODO(M3): genrule for copy_prebuilt_devtools (gn type=action)
cc_library(name = "copy_prebuilt_devtools")

cc_library(
    name = "dartdev",
    deps = [
        "//utils/dartdev:copy_dartdev_snapshot",
        "//utils/dartdev:copy_prebuilt_devtools",
    ],
)

# Group stays a cc_library (//:runtime deps on it): the prebuilt-devtools copy
# stays a deps stub (copy_tree, deferred); the snapshot rides via `data`.
cc_library(
    name = "dartdev_aot",
    deps = [
        "//utils/dartdev:copy_prebuilt_devtools",
    ],
    data = [
        "//utils/dartdev:dartdev_aot_snapshot",
    ],
)

# rules_dart Step 4: real dartdev AOT snapshots. Unblocked by the session-13
# refresh (pkg/tools roll fixed the package:unified_analytics API that
# pkg/dartdev/lib/dartdev.dart imports). Ports the two aot_snapshot() targets in
# utils/dartdev/BUILD.gn over pkg/dartdev/bin/dartdev.dart.

# aot_snapshot("dartdev_aot_snapshot") has force_product_mode = !dart_debug, so
# in this (non-debug) config product = true -> //runtime/bin:gen_snapshot_product.
dart_aot_snapshot(
    name = "dartdev_aot_snapshot",
    main = "//:pkg/dartdev/bin/dartdev.dart",
    sources = "//:dart_package_sources",
    gen_kernel_dill = "//utils/gen_kernel:bootstrap_gen_kernel",
    platform_dill = "//runtime/vm:vm_platform.dill",
    gen_snapshot = "//runtime/bin:gen_snapshot_product",
    product = True,
    out = "dartdev_aot.dart.snapshot",
)

# aot_snapshot("dartdev_aot_profile_snapshot") has no force_product_mode ->
# product = false -> //runtime/bin:gen_snapshot.
dart_aot_snapshot(
    name = "dartdev_aot_profile_snapshot",
    main = "//:pkg/dartdev/bin/dartdev.dart",
    sources = "//:dart_package_sources",
    gen_kernel_dill = "//utils/gen_kernel:bootstrap_gen_kernel",
    platform_dill = "//runtime/vm:vm_platform.dill",
    gen_snapshot = "//runtime/bin:gen_snapshot",
    product = False,
    out = "dartdev_aot_profile.dart.snapshot",
)

# TODO(M3): genrule for generate_dartdev_snapshot (gn type=action)
cc_library(name = "generate_dartdev_snapshot")

# TODO(M3): genrule for generate_dartdev_snapshot_dill (gn type=action)
cc_library(name = "generate_dartdev_snapshot_dill")

