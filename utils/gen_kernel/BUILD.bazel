# AUTO-GENERATED by tools/bazel/translate_gn_desc.py from `gn desc` output.
# Hand-fixes belong in this file (M3); rerun the translator to refresh structural
# bits and re-apply fixes from version control.

load("@rules_cc//cc:defs.bzl", "cc_library")
load("//tools/bazel/dart:defs.bzl", "dart_aot_snapshot", "dart_kernel_snapshot")

package(default_visibility = ["//visibility:public"])

# GN lets one target's sources reach into other BUILD.gn dirs; Bazel
# requires cross-package file refs to be explicitly exported. Bulk-export
# every file in the package so the gn-shaped source lists keep working.
exports_files(glob(
    [
        "**/*.h",
        "**/*.hpp",
        "**/*.hh",
        "**/*.inc",
        "**/*.cc",
        "**/*.cpp",
        "**/*.c",
        "**/*.S",
        "**/*.s",
    ],
    allow_empty = True,
))

# rules_dart Step 0: real bootstrap_gen_kernel.dill, regenerated from the
# current prebuilt SDK (the checked-in out/ blob is stale — wrong kernel format
# version). Mirrors utils/gen_kernel/BUILD.gn:bootstrap_gen_kernel.
dart_kernel_snapshot(
    name = "bootstrap_gen_kernel",
    main = "//:pkg/vm/bin/gen_kernel.dart",
    # rules_dart Step 3: gen_kernel.dart imports only package:vm/kernel/args, all
    # in the dart_pkg_vm closure (17 pkgs). Scoping off the opaque blob is what
    # lets a tool dill be a cache hit when an unrelated package changes — this
    # bootstrap dill is a shared upstream of every tool's kernel compile.
    sources = "//:dart_pkg_vm",
)

dart_aot_snapshot(
    name = "gen_kernel",
    out = "gen_kernel_aot.dart.snapshot",
    gen_kernel_dill = "//utils/gen_kernel:bootstrap_gen_kernel",
    gen_snapshot = "//runtime/bin:gen_snapshot_product",
    main = "//:pkg/vm/bin/gen_kernel.dart",
    platform_dill = "//runtime/vm:vm_platform.dill",
    product = True,
    sources = "//:dart_pkg_vm",
)
