# AUTO-GENERATED by tools/bazel/translate_gn_desc.py from `gn desc` output.
# Hand-fixes belong in this file (M3); rerun the translator to refresh structural
# bits and re-apply fixes from version control.

load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library", "cc_shared_library")

package(default_visibility = ["//visibility:public"])

# GN lets one target's sources reach into other BUILD.gn dirs; Bazel
# requires cross-package file refs to be explicitly exported. Bulk-export
# every file in the package so the gn-shaped source lists keep working.
exports_files(glob(
    ["**/*.h", "**/*.hpp", "**/*.hh", "**/*.inc",
     "**/*.cc", "**/*.cpp", "**/*.c", "**/*.S", "**/*.s"],
    allow_empty = True,
))

# TODO(M3): genrule for compile_dart2wasm_js_compatibility_platform (gn type=action)
cc_library(name = "compile_dart2wasm_js_compatibility_platform")

# TODO(M3): genrule for compile_dart2wasm_platform (gn type=action)
cc_library(name = "compile_dart2wasm_platform")

# TODO(M3): genrule for compile_dart2wasm_standalone_platform (gn type=action)
cc_library(name = "compile_dart2wasm_standalone_platform")

# TODO(M3): copy for dart2wasm_asserts_snapshot (gn type=copy)
cc_library(name = "dart2wasm_asserts_snapshot")

# TODO(M3): genrule for dart2wasm_asserts_snapshot_dill (gn type=action)
cc_library(name = "dart2wasm_asserts_snapshot_dill")

# TODO(M3): genrule for dart2wasm_asserts_snapshot_gen_snapshot (gn type=action)
cc_library(name = "dart2wasm_asserts_snapshot_gen_snapshot")

# TODO(M3): copy for dart2wasm_product_snapshot (gn type=copy)
cc_library(name = "dart2wasm_product_snapshot")

# TODO(M3): genrule for dart2wasm_product_snapshot_dill (gn type=action)
cc_library(name = "dart2wasm_product_snapshot_dill")

# TODO(M3): genrule for dart2wasm_product_snapshot_gen_snapshot (gn type=action)
cc_library(name = "dart2wasm_product_snapshot_gen_snapshot")

# TODO(M3): copy for dart2wasm_snapshot (gn type=copy)
cc_library(name = "dart2wasm_snapshot")

# TODO(M3): genrule for dart2wasm_snapshot_dill (gn type=action)
cc_library(name = "dart2wasm_snapshot_dill")

# TODO(M3): genrule for dart2wasm_snapshot_gen_snapshot (gn type=action)
cc_library(name = "dart2wasm_snapshot_gen_snapshot")

# TODO(M3): genrule for ffi_native_test_wasm_module (gn type=action)
cc_library(name = "ffi_native_test_wasm_module")

cc_library(
    name = "test_wasm_modules",
    deps = [
        "//utils/dart2wasm:ffi_native_test_wasm_module",
    ],
)

