# AUTO-GENERATED by tools/bazel/translate_gn_desc.py from `gn desc` output.
# Hand-fixes belong in this file (M3); rerun the translator to refresh structural
# bits and re-apply fixes from version control.

load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library", "cc_shared_library")
load("//tools/bazel/dart:defs.bzl", "dart_aot_snapshot", "dart_compile_platform")

package(default_visibility = ["//visibility:public"])

# GN lets one target's sources reach into other BUILD.gn dirs; Bazel
# requires cross-package file refs to be explicitly exported. Bulk-export
# every file in the package so the gn-shaped source lists keep working.
exports_files(glob(
    ["**/*.h", "**/*.hpp", "**/*.hh", "**/*.inc",
     "**/*.cc", "**/*.cpp", "**/*.c", "**/*.S", "**/*.s"],
    allow_empty = True,
))

# rules_dart Step 2a (compile_platform web variants). Port the three
# compile_platform() targets in utils/dart2wasm/BUILD.gn (--target=dart2wasm,
# dart2wasm_js_compatibility, dart2wasm_standalone). Same web shape as ddc_platform;
# js_compatibility additionally threads -Ddart.wasm.js_compatibility=true. Output
# filenames follow GN's tool naming. Consumed by //:dart2wasm_platform.
dart_compile_platform(
    name = "compile_dart2wasm_platform",
    sources = "//:dart_package_sources",
    sdk_sources = "//sdk:sdk_library_sources",
    libraries_spec = "org-dartlang-sdk:///lib/libraries.json",
    single_root_base = "sdk",
    platform_args = [
        "--target=dart2wasm",
        "dart:core",
    ],
    deps_outline = "//runtime/vm:vm_platform_outline.dill",
    platform_out = "dart2wasm_platform.dill",
    outline = "dart2wasm_outline.dill",
)

dart_compile_platform(
    name = "compile_dart2wasm_js_compatibility_platform",
    sources = "//:dart_package_sources",
    sdk_sources = "//sdk:sdk_library_sources",
    libraries_spec = "org-dartlang-sdk:///lib/libraries.json",
    single_root_base = "sdk",
    platform_args = [
        "--target=dart2wasm_js_compatibility",
        "-Ddart.wasm.js_compatibility=true",
        "dart:core",
    ],
    deps_outline = "//runtime/vm:vm_platform_outline.dill",
    platform_out = "dart2wasm_js_compatibility_platform.dill",
    outline = "dart2wasm_js_compatibility_outline.dill",
)

dart_compile_platform(
    name = "compile_dart2wasm_standalone_platform",
    sources = "//:dart_package_sources",
    sdk_sources = "//sdk:sdk_library_sources",
    libraries_spec = "org-dartlang-sdk:///lib/libraries.json",
    single_root_base = "sdk",
    platform_args = [
        "--target=dart2wasm_standalone",
        "dart:core",
    ],
    deps_outline = "//runtime/vm:vm_platform_outline.dill",
    platform_out = "dart2wasm_standalone_platform.dill",
    outline = "dart2wasm_standalone_outline.dill",
)

# rules_dart Step 4: real dart2wasm AOT snapshots. Unblocked by the session-13
# refresh. Ports the three aot_snapshot() targets in utils/dart2wasm/BUILD.gn
# over pkg/dart2wasm/bin/dart2wasm.dart. The compile_dart2wasm_*_platform targets
# (web platform dills) and the emscripten wasm_module stay deferred stubs.

# aot_snapshot("dart2wasm_snapshot") name="dart2wasm" -> product = false.
dart_aot_snapshot(
    name = "dart2wasm_snapshot",
    main = "//:pkg/dart2wasm/bin/dart2wasm.dart",
    sources = "//:dart_package_sources",
    gen_kernel_dill = "//utils/gen_kernel:bootstrap_gen_kernel",
    platform_dill = "//runtime/vm:vm_platform.dill",
    gen_snapshot = "//runtime/bin:gen_snapshot",
    product = False,
    out = "dart2wasm.snapshot",
)

# aot_snapshot("dart2wasm_asserts_snapshot") -> product = false, with
# --enable-asserts threaded to BOTH gen_kernel and gen_snapshot (matches GN).
dart_aot_snapshot(
    name = "dart2wasm_asserts_snapshot",
    main = "//:pkg/dart2wasm/bin/dart2wasm.dart",
    sources = "//:dart_package_sources",
    gen_kernel_dill = "//utils/gen_kernel:bootstrap_gen_kernel",
    platform_dill = "//runtime/vm:vm_platform.dill",
    gen_snapshot = "//runtime/bin:gen_snapshot",
    product = False,
    gen_kernel_args = ["--enable-asserts"],
    gen_snapshot_args = ["--enable-asserts"],
    out = "dart2wasm_asserts.snapshot",
)

# aot_snapshot("dart2wasm_product_snapshot") force_product_mode = true ->
# product = true -> //runtime/bin:gen_snapshot_product.
dart_aot_snapshot(
    name = "dart2wasm_product_snapshot",
    main = "//:pkg/dart2wasm/bin/dart2wasm.dart",
    sources = "//:dart_package_sources",
    gen_kernel_dill = "//utils/gen_kernel:bootstrap_gen_kernel",
    platform_dill = "//runtime/vm:vm_platform.dill",
    gen_snapshot = "//runtime/bin:gen_snapshot_product",
    product = True,
    out = "dart2wasm_product.snapshot",
)

# TODO(M3): genrule for ffi_native_test_wasm_module (gn type=action)
cc_library(name = "ffi_native_test_wasm_module")

cc_library(
    name = "test_wasm_modules",
    deps = [
        "//utils/dart2wasm:ffi_native_test_wasm_module",
    ],
)

